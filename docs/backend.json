
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents an individual user of the EduConnect Hub system, who can be either an administrator or a visitor. Stores core authentication-related information (excluding passwords) and user management flags.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The unique institutional email address of the user, obtained from Google authentication. Used for login and identification.",
          "format": "email"
        },
        "fullName": {
          "type": "string",
          "description": "The full name of the user, typically retrieved from their Google profile."
        },
        "role": {
          "type": "string",
          "description": "The assigned role of the user within the system, such as 'admin' or 'visitor'."
        },
        "isBlocked": {
          "type": "boolean",
          "description": "A flag indicating whether the user's access to the system has been blocked by an administrator."
        },
        "blockReason": {
          "type": "string",
          "description": "The reason why the user was blocked, if applicable."
        },
        "blockedAt": {
          "type": "string",
          "description": "Timestamp when the user was blocked.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was first created in the system.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user account's information was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "fullName",
        "role",
        "isBlocked",
        "createdAt",
        "updatedAt"
      ]
    },
    "Session": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Session",
      "type": "object",
      "description": "Tracks a user's active session in the application in real-time.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Session entity (usually the User ID)."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User."
        },
        "email": {
          "type": "string",
          "description": "Email of the logged-in user."
        },
        "fullName": {
          "type": "string",
          "description": "Full name of the logged-in user."
        },
        "role": {
          "type": "string",
          "description": "Role of the user (admin/visitor)."
        },
        "lastActive": {
          "type": "string",
          "description": "Timestamp of the last activity.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "Current online status (online/offline)."
        }
      },
      "required": [
        "id",
        "userId",
        "email",
        "lastActive",
        "status"
      ]
    },
    "Department": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Department",
      "type": "object",
      "description": "Represents a college department that visitors can select during their check-in process. This entity provides a standardized list for visitor input and dashboard filtering.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Department entity."
        },
        "name": {
          "type": "string",
          "description": "The official and unique name of the college department (e.g., 'Computer Science', 'Biology')."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "ReasonForVisit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ReasonForVisit",
      "type": "object",
      "description": "Represents a predefined reason that visitors can select for their visit during the check-in process. This entity supports consistent data collection and enables filtering for reports and the dashboard.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ReasonForVisit entity."
        },
        "reason": {
          "type": "string",
          "description": "A descriptive and unique text detailing the purpose of a visit (e.g., 'Meeting with faculty', 'Library Access', 'Event Attendance')."
        }
      },
      "required": [
        "id",
        "reason"
      ]
    },
    "VisitorClassification": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "VisitorClassification",
      "type": "object",
      "description": "Represents distinct categories that visitors can belong to (e.g., 'Student', 'Faculty', 'Alumni', 'Guest'). This entity enables detailed filtering and segmentation in the admin dashboard's real-time statistics.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the VisitorClassification entity."
        },
        "classification": {
          "type": "string",
          "description": "The unique name of the visitor classification (e.g., 'Student', 'Alumni', 'Guest')."
        }
      },
      "required": [
        "id",
        "classification"
      ]
    },
    "Visit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Visit",
      "type": "object",
      "description": "Records a single instance of a user checking into the facility, capturing timestamps and contextual information. This data is critical for real-time tracking, historical logging, and generating summary reports.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Visit entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who made this visit. (Relationship: User 1:N Visit)"
        },
        "departmentId": {
          "type": "string",
          "description": "Reference to the Department the visitor specified during check-in. (Relationship: Department 1:N Visit)"
        },
        "reasonForVisitId": {
          "type": "string",
          "description": "Reference to the ReasonForVisit the visitor specified during check-in. (Relationship: ReasonForVisit 1:N Visit)"
        },
        "visitorClassificationId": {
          "type": "string",
          "description": "Reference to the VisitorClassification selected for this particular visit. (Relationship: VisitorClassification 1:N Visit)"
        },
        "checkInTime": {
          "type": "string",
          "description": "Timestamp indicating when the user checked into the facility.",
          "format": "date-time"
        },
        "checkOutTime": {
          "type": "string",
          "description": "Timestamp indicating when the user checked out of the facility. This field is null if the user is currently checked in.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current status of the visit (e.g., 'checked-in', 'checked-out'). This helps in real-time visitor tracking."
        }
      },
      "required": [
        "id",
        "userId",
        "departmentId",
        "reasonForVisitId",
        "visitorClassificationId",
        "checkInTime",
        "status"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles."
        }
      },
      {
        "path": "/user_sessions/{sessionId}",
        "definition": {
          "entityName": "Session",
          "schema": {
            "$ref": "#/backend/entities/Session"
          },
          "description": "Tracks active user sessions for real-time monitoring by admins."
        }
      },
      {
        "path": "/roles_admin/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Dedicated collection for admin privileges."
        }
      },
      {
        "path": "/visits/{visitId}",
        "definition": {
          "entityName": "Visit",
          "schema": {
            "$ref": "#/backend/entities/Visit"
          },
          "description": "Records individual visitor check-in events."
        }
      }
    ]
  }
}
